#!/bin/sh
PKG_PATH=$(python -c "from distutils.sysconfig import get_python_lib; print(get_python_lib())")
if [ -z "$VAR" ]; then
  "${VIRTUAL_ENV}/bin/pecan" serve --reload ${PKG_PATH}/anchor/config.py
else
  "/usr/bin/pecan" serve --reload ${PKG_PATH}/anchor/config.py
fi
